Generate v3 CoT corpus with corrected verification and expanded categories

Summary:
- Built `build_corpus_v3.py` script to parse prompts, route to solvers, extract and strictly verify CoT answers against `train.csv`.
- Generated `corpus.jsonl` with 726 rows across 6 verified categories (bit_manipulation, numeral, cipher, unit_conversion, gravity, equation_numeric_deduce).
- Patched solvers to correctly output deterministic CoT for their respective categories, fixing issues with numeral regex mapping and cryptarithm reasoning fallback mapping logic.
- Included robust tests in `test_cot_corpus_v3.py` asserting exact and numerical matching logic, row counts, and category coverage.
- Exported quality metadata to `quality_report.json` as specified.
