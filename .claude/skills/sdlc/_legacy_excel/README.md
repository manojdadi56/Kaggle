# _legacy_excel — REFERENCE ONLY. DO NOT RUN.

These are the original SDLC skill's Excel state-ledger scripts (`scripts/`) and
schemas/templates (`assets/`). In THIS project they are **superseded** by the
git-JSON toolkit (`orchestrator/` + `python -m orchestrator.tools`).

- Do NOT execute `state_ledger.py`, `workbook_*.py`, `role_selector.py`, etc.
- Do NOT create `state.xlsx`.
- Kept only so the role/contract DNA in `../references/` stays self-consistent and
  auditable. The authoritative mapping is `../INTEGRATION.md`.
