# Numeral Solver

Implement solver for arbitrary base numeral arithmetic tasks, reading the prompt and performing the requested transformation (addition, subtraction, conversion) given an alphabet mapping.
